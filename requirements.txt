# Core
numpy>=1.26
pandas>=2.1
pyarrow>=14.0
scipy>=1.11
pyyaml>=6.0

# Modelling
scikit-learn>=1.4
lightgbm>=4.3
statsmodels>=0.14
optuna>=3.6

# Data access
kaggle>=1.6

# Visualisation
matplotlib>=3.8
seaborn>=0.13

# Utilities
tqdm>=4.66
joblib>=1.3

# Notes:
# - The time-series foundation model (Chronos) and the LLM insight layer pull in
#   heavier dependencies (torch, transformers, an LLM SDK). They are kept in
#   requirements-optional.txt so the core forecasting pipeline stays lightweight
#   and installable on CPU-only machines.
